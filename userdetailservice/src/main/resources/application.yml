spring:
  application:
    name: userdetailservice
  datasource:
    url: jdbc:postgresql://localhost:5432/deep_fitness_user_db
    username: root
    password: your_desired_password

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect